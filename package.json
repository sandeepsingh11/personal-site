{
  "name": "personal-site",
  "version": "1.0.0",
  "description": "Sandeep's freelancing site!",
  "main": "index.js",
  "scripts": {
    "dev": "eleventy --serve & tailwindcss -i ./src/css/tailwind.css -o ./_site/css/style.css --watch",
    "prod": "eleventy & tailwindcss -i ./src/css/tailwind.css -o ./_site/css/style.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sandeep S",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@tailwindcss/forms": "^0.5.6",
    "tailwindcss": "^3.3.3"
  }
}
